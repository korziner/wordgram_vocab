# Cargo.toml
[package]
name = "wordgram_vocab"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
zstd = "0.13"
serde_json = "1"
fxhash = "0.2"
rayon = "1.12"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
